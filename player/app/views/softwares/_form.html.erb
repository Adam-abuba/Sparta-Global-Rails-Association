<%= form_with(model: software, local: true) do |form| %>
  <% if software.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(software.errors.count, "error") %> prohibited this software from being saved:</h2>

      <ul>
      <% software.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.text_field :cost %>
  </div>

  <div class="field">
    <%= form.label :functional %>
    <%= form.check_box :functional %>
  </div>

  <div class="field">
    <%= form.label :developer_id %>
    <%=collection_select(:game, :developer_id, Developer.all, :id, :name)%>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%=collection_select(:game, :user_id, User.all, :id, :name)%>
    
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
